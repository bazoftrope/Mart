# Памятка агенту

- Рабочей/прод-базы с данными сейчас нет.
- Новые файлы миграций **не создавать** — это нагромождает файлы.
- Изменения схемы БД вносить правкой уже существующих файлов в `DB/migrations/`.
- Подробнее: `DOC/architecture.md` → «База данных → Миграции».
