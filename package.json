{
  "name": "foodmart-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "deploy": "git push production main",
    "setup-server": "./scripts/setup-server.sh",
    "backend": "cd backend && ./pocketbase serve"
  }
}
